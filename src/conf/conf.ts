
export const conf = {
    googleClientId:String(import.meta.env.VITE_GOOGLE_CLIENT_ID),
    oauthRedirectUri:String(import.meta.env.VITE_OAUTH_REDIRECT_URI),
    googleOauthUri:String(import.meta.env.VITE_GOOGLE_OAUTH_URI),
    serverEndpoint:String(import.meta.env.VITE_SERVER_ENDPOINT),
}
